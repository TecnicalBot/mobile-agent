{
  "name": "mobile-agent-oauth-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Centralized OAuth proxy for Mobile Agent MCP servers (Cloudflare Worker).",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260910.1",
    "typescript": "~6.0.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.7.2"
  },
  "packageManager": "pnpm@10.29.2"
}